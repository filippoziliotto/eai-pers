# -------------------------------------------------------------------
# experiment.yaml
# Only the parameters you’re tuning for this experiment
# -------------------------------------------------------------------

data:
  data_split: "scene_unseen"

training:
  batch_size: 8
  num_epochs: 20

optimizer:
  lr: 5e-4
  type: "adamw"

scheduler:
  type: "cosine_annealing"

augmentations:
  use_aug: true
  use_random_crop: true
  use_random_rotate: true

logging:
  wandb:
    use_wandb: false
    run_name: "none"
