# -------------------------------------------------------------------
# experiment.yaml
# Only the parameters you’re tuning for this experiment
# -------------------------------------------------------------------

data:
  data_split: "scene_unseen"

training:
  batch_size: 2
  num_epochs: 20

optimizer:
  lr: 5e-4
  type: "adamw"

scheduler:
  type: "cosine_annealing"

logging:
  wandb:
    use_wandb: false
    run_name: "none"
